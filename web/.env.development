# Dev only: the API client calls /api/be/* and Next rewrites proxy it to the
# FastAPI backend on :8000 (same-origin in the browser → no CORS). In production
# the app is static-exported and served by FastAPI, so the base is "" (same origin).
NEXT_PUBLIC_API_BASE=/api/be
